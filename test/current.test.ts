import { describe, expect, test } from "@jest/globals"
import {
  numberToNorm,
  normToNumber,
  stringToNumber,
  numberToString,
  checkConfig,
  parseValue,
  Metric,
} from "../src"

test("empty placeholder", () => {})
